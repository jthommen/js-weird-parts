// Strict Mode

'use strict';
// Extra feature, not all JS engines apply it the same way
// Must go on top of file or function
// Must declare variable before setting value

function logNewperson() {
    'use strict';

    var person2;
    persom2 = {};
    console.log(persom2);

}

var person; // creates a new object

persom = {}; // creates a new empty object
console.log(persom);
lowNewPerson();